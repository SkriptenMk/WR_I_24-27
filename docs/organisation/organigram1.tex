\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{%
                positioning, 
                calc, 
                decorations.pathreplacing,
                }

\begin{document}
\tikzset{
    stelle/.style={
        draw,
        rectangle
        },
    stabsstelle/.style={
        stelle,
        append after command={ 
            (\tikzlastnode.north west) edge[double] (\tikzlastnode.south west)
            }
        }
    }
\begin{tikzpicture}
    \node[stelle] (a) {GL};
    \node[stelle, below left= 20mm of a] (b) {D};
    \node[stelle, below right= 20mm of a] (c) {CH};
    \node[stelle, below= 14mm of a] (d) {A};
    \node[below= 10mm of a] (e) {};
    \node[below= 5mm of a] (f) {};

    \node[stabsstelle, right = of f] (g) {Rechtsdienst};

    \draw (a) -- (d);
    \draw (e.mid) -| (b);
    \draw (e.mid) -| (c);
    \draw (g) -- ($(a)!(g)!(e)$);

    \draw[decorate, decoration={brace, mirror, amplitude=4pt}] 
         (b.south west) -- 
         node[below=5pt] {Kontrollspanne} 
         (c.south east);
\end{tikzpicture}
\end{document}
