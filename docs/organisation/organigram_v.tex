\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{%
                positioning, 
                calc, 
                decorations.pathreplacing,
                }

\begin{document}
\tikzset{
    stelle/.style={
        draw,
        rectangle
        },
    stabsstelle/.style={
        stelle,
        append after command={ 
            (\tikzlastnode.north west) edge[double] (\tikzlastnode.south west)
            }
        }
    }
\begin{tikzpicture}
\node[stelle] (a) {GL};
\node[stabsstelle, below right= of a] (b) {Rechtsdienst};
\node[stelle, below= of b.east, anchor=east] (c) {D};
\node[stelle, below= of c.east, anchor=east] (d) {A};
\node[stelle, below= of d.east, anchor=east] (e) {CH};

\draw (a) |- (b);
\draw (a) |- (c);
\draw (a) |- (d);
\draw (a) |- (e);

\draw[decorate, decoration={brace, amplitude=10pt}] 
         (b.north east) -- node[right=10pt] {Kontrollspanne} 
         (e.south east);
\end{tikzpicture}
\end{document}